import createBrowserHistory from 'history/createBrowserHistory';

export default createBrowserHistory(); // --> this function returns history object

// this file is created to share a single instance of the createBrowserhistory func return instance(history object).